<UserControl x:Class="Finalspace.Onigiri.Controls.LoadingBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Finalspace.Onigiri.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
             d:DesignWidth="800"
             d:Background="Black"
             d:Foreground="White"
             x:Name="rootControl"
             Style="{StaticResource userControlStyle}"
             >
    <Grid Margin="5" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="{DynamicResource MaterialDesign.Brush.Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Width="300" TextTrimming="CharacterEllipsis" Text="{Binding LoadingHeader, ElementName=rootControl, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Grid.Row="1" Width="300" TextTrimming="CharacterEllipsis" Text="{Binding LoadingSubject, ElementName=rootControl, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
        <ProgressBar Grid.Row="2" Width="300" Height="20" Value="{Binding LoadingPercentage, ElementName=rootControl, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" IsIndeterminate="{Binding IsLoadingMarque, ElementName=rootControl, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
    </Grid>
</UserControl>
